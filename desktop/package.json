{
  "name": "scriptdeck-desktop",
  "private": true,
  "version": "1.2.0",
  "description": "ScriptDeck 桌面版（Tauri 2 + Rust）",
  "scripts": {
    "sync-ui": "node scripts/sync-ui.mjs",
    "sync-version": "node scripts/sync-version.mjs",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
